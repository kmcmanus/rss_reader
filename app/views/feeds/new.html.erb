<h1>New Subscription</h1>

<%= simple_form_for @feed, :html => {:class => 'form-horizontal'} do |f| %>
  <%= f.input :feed_url %>
  <%= f.button :submit, :class => 'btn btn-primary' %>
  <%= link_to "Cancel", feeds_path, :class => 'btn btn-danger' %>
<%end %>

